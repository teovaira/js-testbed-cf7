let meters = 2

let centimeters = meters * 100
let millimeters = meters * 1000

console.log(`${meters}, ${centimeters}, ${millimeters}`)
console.log(`${meters} meters = ${centimeters} cm = ${millimeters} mm`)